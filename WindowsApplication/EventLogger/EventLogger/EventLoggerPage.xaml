<Page x:Class="EventLogger.EventLoggerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView Name="ErrorListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn  DisplayMemberBinding="{Binding Date}" Header="Date"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Origin}" Header="Origin"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Payload}" Header="Payload"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Grid.Row="1"  Name="CloseButton" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Close Button" Click="CloseButton_OnClick"/>
    </Grid>
</Page>
